import SideNav from "../components/SideNav";

const Attendance = () => {
  return (
    <div className="flex gap-2 h-full">
      <SideNav />
      <h1>Attendance</h1>
    </div>
  );
};

export default Attendance;
